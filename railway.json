{
  "$schema": "https://railway.app/v1/schema.json",
  "name": "smartlearning",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn smartlearning.wsgi",
    "restartPolicy": {
      "type": "on-failure",
      "maxRetries": 10
    }
  }
}
